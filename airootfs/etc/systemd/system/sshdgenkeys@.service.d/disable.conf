[Unit]
# Disable sshdgenkeys@ template during live boot to prevent early key generation errors
Condition=false

[Service]
# Neutralize any ExecStart defined by the original unit
ExecStart=
ExecStart=/bin/true
Type=oneshot
RemainAfterExit=yes

[Install]
# Ensure no enablement via targets in the live environment
WantedBy=
