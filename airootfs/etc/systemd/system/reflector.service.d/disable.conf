[Unit]
# Disable reflector service completely to prevent hanging during ISO boot
# This prevents reflector from running automatically and blocking the boot process
Condition=false

[Service]
# Ensure the service cannot be started
ExecStart=
ExecStart=/bin/true
Type=oneshot
RemainAfterExit=yes

[Install]
# Remove any install dependencies
WantedBy=
